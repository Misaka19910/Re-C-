#include <stdio.h>

int main ()

{
    //计算两个值之间的关系，叫做关系运算；
    //如：== 相等，！=不相等，>大于,<小于，>=大于或等于，<=小于或等于；
    printf("%d\n",5<6);
    printf("%d\n", 4==5);
    //如若条件成立，输出1.如若条件不成立，输出0；
    //关系运算符优先级低于计算运算；
    printf("%d\n", 7>= 3 + 4)
    
    return 0;

}